# 验收记录

## 0.1.1 修复验证（2026-09-06，进行中）

- 已在 v0.1.0 原镜像中复现：正常提交自定义 README 内容、工作树干净，旧版仍不通过。
- 同一测试发行版执行课程升级后通过，HEAD 与提交历史未变化；另一练习的未暂存草稿、暂存内容和 Windows 进度文件保持不变。
- 5 项 Node 测试通过，覆盖显示比例边界及 WSL 服务故障不能误触发组件安装。
- Python 语法、Vite 构建、格式与空白检查通过；npm audit 未发现已知漏洞。0.1.1 NSIS 候选安装包构建成功，镜像和课程更新包校验通过；尚未发布为新版本。
- Electron 排版检查使用专门的固定测试数据，不执行 Git：1280、1536、2560 宽窗口及 1536/1080 宽的 200% 缩放均无文档横向溢出。正文/编辑器实测 16px，可见文字无小于 12px 的项。此检查不能代替真实终端测试。
- 当前 WSL 整体无响应：`wsl --list --quiet` 与测试发行版的 `/bin/echo` 均超时；因此新版完整 Linux/PTY 回归及真实桌面端到端测试尚未完成。重启 WSL 会影响用户其他 Linux 程序，尚未执行。
- 以下为 **0.1.0 历史验收记录**，不代表上述新版测试已通过。

日期：2026-09-06。下列记录将随本次实现完成更新，未经执行的项目不标记通过。

## 当前实测环境

- Windows 11 专业版 x64，系统版本 10.0.26200。
- WSL 2.6.1，Linux 内核 6.6.87.2-microsoft-standard-WSL2。
- Node.js 24.19.0。
- Alpine 3.24.1 镜像构建并安装当前仓库更新；Git 2.54.0。

## 已通过

- Node 逻辑测试：无效输入拒绝、WSL 输出编码、真实父子关系图布局，3 项。
- 专用 WSL 镜像导入与 sandbox 运行。
- 六个课程场景，包含实际保存、暂存、提交、分支、fetch、冲突、merge、push、restore、revert。
- 错误中间状态不通过：仅保存、仅暂存、未推送、未恢复配置。
- 恢复关草稿保留、恢复已提交配置且保留原历史。
- 继续练习保留仓库，显式重置恢复初始场景。
- Linux 路径穿越、symlink、FIFO 处理及隔离探测；合计 57 个断言。
- 真正 PTY：进入仓库、Bash 执行、37×111 尺寸调整、Ctrl+C 中断。
- npm 安装后审计：0 个已知漏洞。

## 桌面与交付验证

- Electron 源码运行的实际界面测试通过：初始化、编辑与保存、错误命令反馈、真实终端提交、状态与提交图更新、取消重置、未保存编辑保护、退出后恢复。
- NSIS 安装包构建成功：139,078,540 字节（约 132.6 MiB），内置 Linux 镜像通过 SHA256 校验。
- 将实际安装包安装到带空格的 Windows 路径，安装器退出码为 0。
- 使用安装后的 `Git Onboarding.exe` 再运行同一套端到端测试，全部通过；应用数据路径包含空格和英文单引号，验证 WSL 导入与参数传递兼容性。
- 安装包 Authenticode 检查为未签名，已在 README 明示。
- GitHub 仓库 `LX-Cel/git-onboarding` 可见性为 PRIVATE，经 SSH 推送。
- GitHub Actions 的 Windows 验证运行通过：依赖安装、3 项逻辑测试、Python 语法、网页构建、依赖审计。

实际界面截图位于 [screenshots](screenshots/)。页面使用本地实际仓库状态；首页说明插图是示意，提交图由真实 Git 历史生成。

## 尚未覆盖

- 全新未安装 WSL 的 Windows、管理员拒绝授权、BIOS 虚拟化关闭、企业策略限制、实际重启后的系统组件初始化。
- Windows 家庭版、Windows 10、ARM64、其他操作系统。
- 代码签名、自动更新、恶意内核逃逸、全局资源配额、卸载数据清理。

CI 验证 JavaScript 逻辑、Python 语法、网页构建和依赖审计；不会将普通 CI 构建通过描述为 WSL 实机或安装器验证通过。
