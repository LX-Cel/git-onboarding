# 验收记录

## 0.2.0-preview.1 发布验证（2026-09-13）

- 从已完成的 `d02b073` 课程代码构建；33 门课程、66 个引导/挑战场景。正在开发的 PR 增强模块未包含。
- 475 项真实 Git 断言、隔离检查、真实 PTY 以及 6 项 Node 测试通过。
- 同批 NSIS 构建出的 `win-unpacked/Git Onboarding.exe` 通过全部 4 项 Electron 测试，覆盖子模块、部分暂存/忽略规则、双远端 PR、入门提交/重置/进度/缩放。测试使用项目专属环境。
- 旧 0.1.0 镜像升级至 0.2.0 课程后，提交、暂存区、工作区和进度保留。桌面程序版本为 0.2.0-preview.1，课程版本为 0.2.0。
- 构建、镜像与课程代码校验、格式检查通过。Vite 仍有超过 500 kB 的 JS 包提示，构建成功。
- 安装包 `Git-Onboarding-0.2.0-preview.1-Setup.exe`：139,158,112 字节，SHA256 `2414f444416f0abeac6e05241115ba41588cfa73f0e6dcd13bc6c30f29433c0b`，Authenticode 为 NotSigned。
- 未覆盖用户实际安装来测试安装器升级，也未新增平台验收；此预览版不代表完整覆盖矩阵完成。

## 0.1.2 Fluent 2 界面验证（2026-09-07）

- 按用户选定的 J 概念稿重做实际 React 界面：浅蓝导航与窗口底层、白色编辑器、深色完整终端、右侧任务/仓库状态/提交图。首页、初始化、确认与检查弹窗使用同一套样式。
- 源码运行和 NSIS 打包产物内的 `win-unpacked/Git Onboarding.exe` 均通过真实 Electron 端到端测试：保存、自定义内容提交、错误命令反馈、两次重复手动检查、未保存保护、提交图、恢复进度与125%字号、引导到独立挑战、通过右侧下一关进入协作关。
- 1536×1000、1280×800、1080×720 窗口检查无文档横向溢出，编辑器、终端和右侧面板不重叠；200% 缩放检查通过。正文和编辑器计算字号16px，终端配置16px。高倍缩放采用纵向滚动布局，不代表所有面板能在一屏同时显示。
- 测试复用 `.local/ui-after-wsl-restart` 内的专用测试发行版，没有操作用户实际练习目录。首次测试暴露测试用例对“开始练习始终进入第一关”的错误假设，已改成显式选择第一关，后续源码和打包程序测试通过。
- 6 项 Node 测试、六场景63个真实运行时断言、隔离检查与PTY目录/Bash/resize/Ctrl+C测试通过；Vite构建、格式和`git diff --check`通过。Vite仍提示单个JS包超过500kB，构建成功，未在本次界面修改中引入拆包工程。
- 桌面版本0.1.2与课程版本0.1.1分开维护。新增兼容性回归：现有0.1.1课程被接受，旧0.1.0课程仍要求更新；镜像哈希与课程文件仍校验。镜像SHA256保持`7177967c1caae681335deabab272213054cb96ac8e0747d694ada9ea02187f2a`。
- NSIS安装包生成：`Git-Onboarding-0.1.2-Setup.exe`，139,090,153字节，SHA256 `d6347a510451da90d9242467dd067791ed907b8c7a582e10ca884454171f3b38`。Authenticode状态`NotSigned`。
- 未覆盖用户现有安装来实测安装器升级；本轮实际验证的是安装包同批构建出的程序以及保留0.1.1课程的运行。没有声称完整WCAG审计、原生Mica能力或新增平台支持。
- [实际独立挑战界面](screenshots/fluent-challenge.png)、[手动检查反馈](screenshots/check-result.png)。

## 0.1.1 修复验证（2026-09-06）

- 已在 v0.1.0 原镜像中复现：正常提交自定义 README 内容、工作树干净，旧版仍不通过。
- 同一测试发行版执行课程升级后通过，HEAD 与提交历史未变化；另一练习的未暂存草稿、暂存内容和 Windows 进度文件保持不变。
- 5 项 Node 测试通过，覆盖显示比例边界及 WSL 服务故障不能误触发组件安装。
- Python 语法、Vite 构建、格式与空白检查通过；npm audit 未发现已知漏洞。0.1.1 NSIS 安装包构建成功，139,088,515 字节，镜像和课程更新包校验通过。
- Electron 排版检查使用专门的固定测试数据，不执行 Git：1280、1536、2560 宽窗口及 1536/1080 宽的 200% 缩放均无文档横向溢出。正文/编辑器实测 16px，可见文字无小于 12px 的项。此检查不能代替真实终端测试。
- 完整真实环境回归通过：六个课程场景、63 个断言、隔离检查；PTY 的 Bash 执行、尺寸调整、Ctrl+C 中断通过。
- Electron 源码运行及打包后的 `Git Onboarding.exe` 各自通过真实桌面端到端测试：全新环境初始化、编辑保存、自定义内容提交并完成练习、提交图、未保存编辑保护、重开后保留进度和 125% 字号、200% 布局无横向溢出。打包程序数据目录包含空格和英文单引号。
- 原镜像升级测试再次通过，确认保留提交、暂存内容、工作区草稿和学习进度。
- 测试期间 WSL 全局服务卡住，连 `wsl --shutdown` 也超时；经用户授权重启 WSL 服务后恢复。一次桌面测试因之前中断的导入残留而失败（0x8000000d），保留现场，使用全新测试目录后通过。没有修改用户实际练习仓库，也没有据此声称已定位 WSL 内部死锁原因。
- 0.1.1 安装器已生成，但没有覆盖用户现有安装进行安装器升级实测；实际验证的是打包程序及独立测试发行版中的课程升级。安装包仍未签名。
- 以下为 **0.1.0 历史验收记录**。

日期：2026-09-06。下列记录将随本次实现完成更新，未经执行的项目不标记通过。

## 当前实测环境

- Windows 11 专业版 x64，系统版本 10.0.26200。
- WSL 2.6.1，Linux 内核 6.6.87.2-microsoft-standard-WSL2。
- Node.js 24.19.0。
- Alpine 3.24.1 镜像构建并安装当前仓库更新；Git 2.54.0。

## 已通过

- Node 逻辑测试：无效输入拒绝、WSL 输出编码、真实父子关系图布局，3 项。
- 专用 WSL 镜像导入与 sandbox 运行。
- 六个课程场景，包含实际保存、暂存、提交、分支、fetch、冲突、merge、push、restore、revert。
- 错误中间状态不通过：仅保存、仅暂存、未推送、未恢复配置。
- 恢复关草稿保留、恢复已提交配置且保留原历史。
- 继续练习保留仓库，显式重置恢复初始场景。
- Linux 路径穿越、symlink、FIFO 处理及隔离探测；合计 57 个断言。
- 真正 PTY：进入仓库、Bash 执行、37×111 尺寸调整、Ctrl+C 中断。
- npm 安装后审计：0 个已知漏洞。

## 桌面与交付验证

- Electron 源码运行的实际界面测试通过：初始化、编辑与保存、错误命令反馈、真实终端提交、状态与提交图更新、取消重置、未保存编辑保护、退出后恢复。
- NSIS 安装包构建成功：139,078,540 字节（约 132.6 MiB），内置 Linux 镜像通过 SHA256 校验。
- 将实际安装包安装到带空格的 Windows 路径，安装器退出码为 0。
- 使用安装后的 `Git Onboarding.exe` 再运行同一套端到端测试，全部通过；应用数据路径包含空格和英文单引号，验证 WSL 导入与参数传递兼容性。
- 安装包 Authenticode 检查为未签名，已在 README 明示。
- GitHub 仓库 `LX-Cel/git-onboarding` 可见性为 PRIVATE，经 SSH 推送。
- GitHub Actions 的 Windows 验证运行通过：依赖安装、3 项逻辑测试、Python 语法、网页构建、依赖审计。

实际界面截图位于 [screenshots](screenshots/)。页面使用本地实际仓库状态；首页说明插图是示意，提交图由真实 Git 历史生成。

## 尚未覆盖

- 全新未安装 WSL 的 Windows、管理员拒绝授权、BIOS 虚拟化关闭、企业策略限制、实际重启后的系统组件初始化。
- Windows 家庭版、Windows 10、ARM64、其他操作系统。
- 代码签名、自动更新、恶意内核逃逸、全局资源配额、卸载数据清理。

CI 验证 JavaScript 逻辑、Python 语法、网页构建和依赖审计；不会将普通 CI 构建通过描述为 WSL 实机或安装器验证通过。
