# 首版验收记录

日期：2026-09-06。下列记录将随本次实现完成更新，未经执行的项目不标记通过。

## 当前实测环境

- Windows 11 专业版 x64，系统版本 10.0.26200。
- WSL 2.6.1，Linux 内核 6.6.87.2-microsoft-standard-WSL2。
- Node.js 24.19.0。
- Alpine 3.24.1 镜像构建并安装当前仓库更新；Git 2.54.0。

## 已通过

- Node 逻辑测试：无效输入拒绝、WSL 输出编码、真实父子关系图布局，3 项。
- 专用 WSL 镜像导入与 sandbox 运行。
- 六个课程场景，包含实际保存、暂存、提交、分支、fetch、冲突、merge、push、restore、revert。
- 错误中间状态不通过：仅保存、仅暂存、未推送、未恢复配置。
- 恢复关草稿保留、恢复已提交配置且保留原历史。
- 继续练习保留仓库，显式重置恢复初始场景。
- Linux 路径穿越、symlink、FIFO 处理及隔离探测；合计 57 个断言。
- 真正 PTY：进入仓库、Bash 执行、37×111 尺寸调整、Ctrl+C 中断。
- npm 安装后审计：0 个已知漏洞。

## 待本次验证完成填写

- Electron 实际界面端到端测试。
- NSIS 安装包构建、安装与启动。
- GitHub 私有可见性、远端提交与本地一致性。

## 尚未覆盖

- 全新未安装 WSL 的 Windows、管理员拒绝授权、BIOS 虚拟化关闭、企业策略限制、实际重启后的系统组件初始化。
- Windows 家庭版、Windows 10、ARM64、其他操作系统。
- 代码签名、自动更新、恶意内核逃逸、全局资源配额、卸载数据清理。

CI 验证 JavaScript 逻辑、Python 语法、网页构建和依赖审计；不会将普通 CI 构建通过描述为 WSL 实机或安装器验证通过。
